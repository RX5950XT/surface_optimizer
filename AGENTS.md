# 專案規範

## 語言與回覆

- 一律使用繁體中文（臺灣用語），只講結果；工具、函式與指令保留原文。
- 能直接完成就直接做；最終回報附實際驗證指令與輸出。
- 非簡單任務結束時交代：做了什麼、未完成事項、需要決定事項（後兩者沒有就省略）。

## 工作方式

- 動手前掃描可用 skills；撰寫、審查或重構程式碼前，先讀取並遵循 `karpathy-guidelines`。
- 非簡單任務先在 `tasks/todo.md` 寫可勾選計畫，過程中更新，最後補 Review。
- 先讀完資料流與所有相關 caller，再做根本原因的最小修改；不要為未來需求增加抽象。
- 每次修改都要自行驗證；同一修法失敗兩次，改用別的方法。
- 保留工作樹中與本次無關的變更，禁止使用破壞性 Git 指令。

## 程式與安全

- 優先不可變資料；函式盡量小於 50 行、檔案盡量小於 800 行、巢狀不超過 4 層。
- 外部輸入都要驗證；錯誤不可靜默吞掉，邊界要回傳結構化錯誤。
- 不可硬編碼 secrets；新增依賴前檢查漏洞、維護狀態並鎖定版本。
- 新 API 回應使用 `success/status`、`data`、`error`；分頁另加 `metadata`。

## 文件與 Git

- README 使用繁體中文。規則或架構有實質變動時，同步維護精簡的 `CLAUDE.md`、`AGENTS.md` 與根目錄 `CONTEXT.md`。
- Commit 格式：`<type>: <description>`；可用 `feat`、`fix`、`refactor`、`docs`、`test`、`chore`、`perf`、`ci`。
